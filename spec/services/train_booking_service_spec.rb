require "rails_helper"

describe TrainBookingService do
  let(:train) { subject }

  describe "#train to be scheduled" do
    it "testing headline tagging" do
      expect(true).to eq(true)
    end
  end
end
